---
import Container from './Container.astro'
import { homeSectionStyle } from '../styles/styles'
---

<section class={`${homeSectionStyle} min-h-screen`}>
    <Container 
      class="flex flex-col items-center justify-center gap-8 min-w-0"
      showTitlebar={true}
      title='fabiodicec.ca'
    >
      <slot />
    </Container>
</section>