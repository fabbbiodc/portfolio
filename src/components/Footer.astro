---
import { footerButtonStyle } from "../styles/styles";
---

<footer class="absolute bottom-0 left-0 right-0 p-4 flex justify-start gap-6 z-10 text-xs">
  <button class={`${footerButtonStyle} whitespace-nowrap`} data-action="open-window" data-target="impressum">> impressum</button>
  <button class={`${footerButtonStyle} whitespace-nowrap`} data-action="open-window" data-target="credits">> credits</button>

</footer>