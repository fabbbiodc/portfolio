---
// Draggable window

import Titlebar from "./Titlebar.astro";
import { draggableWindowStyle, containerContent } from "../styles/styles";

interface Props {
  id: string;
  title: string;
  initialX?: number;
  initialY?: number;
  closable?: boolean;
  initiallyHidden?: boolean;
  noPadding?: boolean;
}

const { id, title, initialX = 100, initialY = 100, closable = true, initiallyHidden = false, noPadding = false } = Astro.props;
---

<div 
  class={`${draggableWindowStyle}`}
  data-window-id={id}
  data-closable={closable ? "true" : "false"}
  style={`transform: translate(${initialX}px, ${initialY}px);${initiallyHidden ? ' display: none;' : ''}`}
>
  <Titlebar title={title} closable={closable} />
  <div class={`${containerContent} ${noPadding ? '' : 'p-6 md:p-12'}`}>
    <slot />
  </div>
</div>

<script>
  import "../scripts/draggable.ts";
</script>
